version: 1.0
runtime: nodejs22

build:
  commands:
    build:
      - npm ci
      - npx prisma generate
      - npm run build

run:
  command: sh -c "npx prisma migrate deploy && node dist/main.js"
  network:
    port: 3001
